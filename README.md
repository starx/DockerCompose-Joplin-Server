# Deploy Joplin Server (via docker compose)

This is docker compose setup which allows to deploy Joplin server only.

## Usage

* Create an `.env` file with the required details.
* Run `docker compose up -d`